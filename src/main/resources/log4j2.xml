<?xml version="1.0" encoding="UTF-8"?>

<Configuration>

	<Properties>
		<!-- Global pattern layout for log messages -->
		<Property name="appenderPatternLayout">%d{YYYY-MM-dd HH:mm:ss.SSS} [%t]
			%-5p %logger{64}.%M : %msg%n</Property>
	</Properties>

	<Appenders>

		<Console name="STDOUT" target="SYSTEM_OUT"
			ignoreExceptions="false">
			<PatternLayout pattern="${appenderPatternLayout}" />
		</Console>

		<RollingFile name="RollingFile" filename="logs/chess.log">

			<PatternLayout pattern="${appenderPatternLayout}" />

			<Policies>
				<SizeBasedTriggeringPolicy size="10 MB" />
			</Policies>

			<DefaultRolloverStrategy max="20" />

		</RollingFile>

	</Appenders>

	<Loggers>
	
		<Root level="OFF" additivity="false">
			<AppenderRef ref="STDOUT" />
			<AppenderRef ref="RollingFile" />
		</Root>
		
		<Logger name="lld.chess" level="OFF" additivity="false">
			<AppenderRef ref="STDOUT" />
			<AppenderRef ref="RollingFile" />
		</Logger>
		
	</Loggers>	

</Configuration>